/**
 * You need to create a migration to update the enum on the database too!
 */
export enum DegreeEnum {
	ASSOCIATE = "ASSOCIATE",
	BACHELOR = "BACHELOR",
	MASTER = "MASTER",
	DOCTORAL = "DOCTORAL",
}
